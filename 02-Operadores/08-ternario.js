// Los operadores ternarios (en realidad, el operador ternario) son una forma corta y clara de escribir un if...else en una sola línea

// condición ? valor_si_true : valor_si_false;

let edad = 15; 

let acceso = edad > 17 ? 'Permite el ingreso' : 'No permitas el inrego, este usuario es menor';  
console.log(acceso);